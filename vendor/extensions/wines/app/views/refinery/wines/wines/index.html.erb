<% content_for :body do %>
  <ul id="wines">
    <% @wines.each do |wine| %>
      <li>
        <%= link_to wine.vintage, refinery.wines_wine_path(wine) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
